Dashboard runs on:

http://192.168.99.100:30000


Setting preferred namespace:
$ kubectl config set-context $(kubectl config current-context) --namespace=<insert-namespace-name-here>
# Validate it
$ kubectl config view | grep namespace:


elk stack:

felix:~/Projects/cloud-playground/elk$ sysctl vm.max_map_count
vm.max_map_count = 65530

https://www.informatik-aktuell.de/entwicklung/methoden/kubernetes-funktioniert-das-wirklich.html

# kubectl autocompletion:
source <(kubectl completion bash)

# Shutdown k8s pods gracefuly
https://pracucci.com/graceful-shutdown-of-kubernetes-pods.html

# Run local docker repo
docker run -d -v /home/registry:/var/lib/registry -p 5000:5000 --name registry registry:2
