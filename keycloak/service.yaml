apiVersion: v1
kind: Service
metadata:
  name: keycloak
  labels:
    run: keycloak
spec:
  selector:
    app: keycloak
  ports:
  - port: 8080
    name: service
    protocol: TCP
    nodePort: 30111
  type: NodePort
