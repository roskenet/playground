apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    run: gateway
spec:
  selector:
    app: gateway
  ports:
  - port: 8080
    name: service
    protocol: TCP
    nodePort: 30300
  type: NodePort
